# conf/config.yaml

###############################################################################
# Project Configuration for a GWAS Pipeline
#
# This file centralizes all configurable parameters, input/output paths,
# and tool settings for a reproducible Genome-Wide Association Study.
#
# All paths within 'data_paths' are relative to the project root directory.
###############################################################################

# --- Project Metadata ---
project:
  name: "CN_estimator"
  id: "2025-06-25_CN_estimator" # Unique identifier for this specific run/project
  version: "1.0.0"
  description: "Pipeline to estimate copy number of specific region in the genome"
  owner: "Paras Garg"
  contact_email: "paras.garg@mssm.edu"
  date_generated: "2025-06-25" # Automatically updated if generated by script


# --- Data Paths (Relative to project root) ---
data_paths:
  # Reference Data (Large files, might be symlinked or external)
  reference_fasta: "resources/hg38/Homo_sapiens_assembly38.fasta" 